name: Power BI - Validate

on:
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Criar relatório placeholder
        run: |
          mkdir -p artifacts
          echo "Status: PASS (placeholder)" > artifacts/quality-summary.md

      - uses: actions/upload-artifact@v4
        with:
          name: quality-report
          path: artifacts
